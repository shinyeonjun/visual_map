services:
  postgres:
    image: postgres:16-alpine
    container_name: dbmcp-postgres
    environment:
      POSTGRES_USER: dbmcp
      POSTGRES_PASSWORD: dbmcp
      POSTGRES_DB: dbmcp
    ports:
      - "15432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dbmcp -d dbmcp"]
      interval: 2s
      timeout: 3s
      retries: 30

  postgres14:
    image: postgres:14-alpine
    container_name: dbmcp-postgres14
    environment:
      POSTGRES_USER: dbmcp
      POSTGRES_PASSWORD: dbmcp
      POSTGRES_DB: dbmcp
    ports:
      - "15434:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dbmcp -d dbmcp"]
      interval: 2s
      timeout: 3s
      retries: 30

  postgres15:
    image: postgres:15-alpine
    container_name: dbmcp-postgres15
    environment:
      POSTGRES_USER: dbmcp
      POSTGRES_PASSWORD: dbmcp
      POSTGRES_DB: dbmcp
    ports:
      - "15435:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dbmcp -d dbmcp"]
      interval: 2s
      timeout: 3s
      retries: 30

  postgres17:
    image: postgres:17-alpine
    container_name: dbmcp-postgres17
    environment:
      POSTGRES_USER: dbmcp
      POSTGRES_PASSWORD: dbmcp
      POSTGRES_DB: dbmcp
    ports:
      - "15437:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dbmcp -d dbmcp"]
      interval: 2s
      timeout: 3s
      retries: 30

  postgres18:
    image: postgres:18-alpine
    container_name: dbmcp-postgres18
    environment:
      POSTGRES_USER: dbmcp
      POSTGRES_PASSWORD: dbmcp
      POSTGRES_DB: dbmcp
    ports:
      - "15438:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dbmcp -d dbmcp"]
      interval: 2s
      timeout: 3s
      retries: 30

  yugabytedb:
    image: yugabytedb/yugabyte:2025.2.3.2-b1
    container_name: dbmcp-yugabyte
    command: ["bin/yugabyted", "start", "--background=false", "--callhome=false"]
    ports:
      - "15443:5433"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "bin/ysqlsh -h $$(hostname) -U yugabyte -d yugabyte -c 'SELECT 1' >/dev/null 2>&1",
        ]
      interval: 5s
      timeout: 5s
      retries: 60
      start_period: 180s

  mysql:
    image: mysql:8.4
    container_name: dbmcp-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_ROOT_HOST: "%"
      MYSQL_DATABASE: dbmcp
      MYSQL_USER: dbmcp
      MYSQL_PASSWORD: dbmcp
    ports:
      - "127.0.0.1:13306:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-udbmcp", "-pdbmcp"]
      interval: 2s
      timeout: 5s
      retries: 60

  mysql80:
    image: mysql:8.0
    container_name: dbmcp-mysql80
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_ROOT_HOST: "%"
      MYSQL_DATABASE: dbmcp
      MYSQL_USER: dbmcp
      MYSQL_PASSWORD: dbmcp
    ports:
      - "127.0.0.1:13308:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-udbmcp", "-pdbmcp"]
      interval: 2s
      timeout: 5s
      retries: 60

  mysql97:
    image: mysql:9.7
    container_name: dbmcp-mysql97
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_ROOT_HOST: "%"
      MYSQL_DATABASE: dbmcp
      MYSQL_USER: dbmcp
      MYSQL_PASSWORD: dbmcp
    ports:
      - "127.0.0.1:13309:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-udbmcp", "-pdbmcp"]
      interval: 2s
      timeout: 5s
      retries: 60

  mariadb1011:
    image: mariadb:10.11
    container_name: dbmcp-mariadb1011
    environment:
      MARIADB_ROOT_PASSWORD: root
      MARIADB_ROOT_HOST: "%"
      MARIADB_DATABASE: dbmcp
      MARIADB_USER: dbmcp
      MARIADB_PASSWORD: dbmcp
    ports:
      - "127.0.0.1:13311:3306"
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 2s
      timeout: 5s
      retries: 60

  mariadb114:
    image: mariadb:11.4
    container_name: dbmcp-mariadb114
    environment:
      MARIADB_ROOT_PASSWORD: root
      MARIADB_ROOT_HOST: "%"
      MARIADB_DATABASE: dbmcp
      MARIADB_USER: dbmcp
      MARIADB_PASSWORD: dbmcp
    ports:
      - "127.0.0.1:13314:3306"
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 2s
      timeout: 5s
      retries: 60

  mariadb118:
    image: mariadb:11.8
    container_name: dbmcp-mariadb118
    environment:
      MARIADB_ROOT_PASSWORD: root
      MARIADB_ROOT_HOST: "%"
      MARIADB_DATABASE: dbmcp
      MARIADB_USER: dbmcp
      MARIADB_PASSWORD: dbmcp
    ports:
      - "127.0.0.1:13318:3306"
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 2s
      timeout: 5s
      retries: 60

  mariadb123:
    image: mariadb:12.3
    container_name: dbmcp-mariadb123
    environment:
      MARIADB_ROOT_PASSWORD: root
      MARIADB_ROOT_HOST: "%"
      MARIADB_DATABASE: dbmcp
      MARIADB_USER: dbmcp
      MARIADB_PASSWORD: dbmcp
    ports:
      - "127.0.0.1:13323:3306"
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 2s
      timeout: 5s
      retries: 60

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: dbmcp-sqlserver
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "Password123!"
    ports:
      - "11433:1433"

  sqlserver2017:
    image: mcr.microsoft.com/mssql/server:2017-latest
    container_name: dbmcp-sqlserver2017
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "Password123!"
    ports:
      - "11437:1433"

  sqlserver2019:
    image: mcr.microsoft.com/mssql/server:2019-latest
    container_name: dbmcp-sqlserver2019
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "Password123!"
    ports:
      - "11439:1433"

  sqlserver2025:
    image: mcr.microsoft.com/mssql/server:2025-latest
    container_name: dbmcp-sqlserver2025
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "Password123!"
    ports:
      - "11435:1433"

  oracle:
    image: gvenzl/oracle-free:23-slim-faststart
    container_name: dbmcp-oracle
    environment:
      ORACLE_PASSWORD: oracle
    ports:
      - "11521:1521"
