{
  "schema": "code-memory.framework-fixture.v1",
  "language": "python",
  "framework": "fastapi",
  "files": [
    {
      "path": "src/fixture.py",
      "source": "from fastapi import FastAPI\napp = FastAPI()\n@app.get('/fixture')\ndef handler(): pass\n@app.websocket('/fixture/ws')\nasync def stream(websocket): pass\n@Autowired UserService service;"
    },
    {
      "path": "pyproject.toml",
      "source": "[project]\nname = \"code-memory-framework-fixture\"\ndependencies = [\"fastapi\", \"@app.get\", \"@app.websocket\", \"APIRouter\"]\n# signal: fastapi\n# signal: @app.get\n# signal: @app.websocket\n# signal: APIRouter\n"
    }
  ],
  "expected": {
    "facts": [
      "HTTP_ROUTE",
      "DEPENDENCY"
    ],
    "relations": [
      "HANDLES"
    ]
  }
}
