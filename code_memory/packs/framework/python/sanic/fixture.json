{
  "schema": "code-memory.framework-fixture.v1",
  "language": "python",
  "framework": "sanic",
  "files": [
    {
      "path": "src/fixture.py",
      "source": "from sanic import Sanic\n@app.get('/fixture')\nasync def handler(request): pass\napp.use(authMiddleware);"
    },
    {
      "path": "pyproject.toml",
      "source": "[project]\nname = \"code-memory-framework-fixture\"\ndependencies = [\"sanic\", \"@app.get\", \"Sanic(\"]\n# signal: sanic\n# signal: @app.get\n# signal: Sanic(\n"
    }
  ],
  "expected": {
    "facts": [
      "HTTP_ROUTE",
      "MIDDLEWARE"
    ],
    "relations": [
      "HANDLES"
    ]
  }
}
