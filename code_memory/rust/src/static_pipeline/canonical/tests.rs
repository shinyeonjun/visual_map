//! Canonical-linker behavior is exercised through the authoritative
//! Language-IR fixture in `static_pipeline::language_ir::tests`; focused
//! negative and bundle tests live here as the linker contract grows.
