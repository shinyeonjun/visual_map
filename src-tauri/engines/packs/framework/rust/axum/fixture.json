{
  "schema": "code-memory.framework-fixture.v1",
  "language": "rust",
  "framework": "axum",
  "files": [
    {
      "path": "src/fixture.rs",
      "source": "Router::new().route('/fixture', get(handler));\napp.use(authMiddleware);"
    },
    {
      "path": "Cargo.toml",
      "source": "[package]\nname = \"code-memory-framework-fixture\"\nversion = \"1.0.0\"\n\n[dependencies]\n# signal: use axum\n# signal: :new\n# signal: .route(\n"
    }
  ],
  "expected": {
    "facts": [
      "HTTP_ROUTE",
      "MIDDLEWARE"
    ],
    "relations": [
      "HANDLES"
    ]
  }
}
